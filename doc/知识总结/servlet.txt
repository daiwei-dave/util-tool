




不错的教程：https://www.ibm.com/developerworks/cn/java/j-lo-servlet/


官方文档翻译
https://www.gitbook.com/book/waylau/servlet-3-1-specification/details
1.Servlet生命周期

init方法（tomcat启动时就会调用）： 是在servlet实例创建时调用的方法，用于创建或打开任何与servlet相的资源和初始 化servlet的状态，
Servlet规范保证调用init方法前不会处理任何请求 
service方法：是servlet真正处理客户端传过来的请求的方法，由web容器调用， 根据HTTP请求方法（GET、POST等），将请求分发到doGet、doPost等方法 
1.service()是在javax.servlet.Servlet接口中定义的
2.service判断请求类型，决定是调用doGet还是doPost方法
3.doGet/doPost 则是在 javax.servlet.http.HttpServlet 中实现的
destory方法：是在servlet实例被销毁时由web容器调用。Servlet规范确保在destroy方法调用之 前所有请求的处理均完成，



Servlet是线程不安全的，在Servlet类中可能会定义共享的类变量，
这样在并发的多线程访问的情况下，不同的线程对成员变量的修改会引发错误。

2.Web service
Web service顾名思义是基于web的服务，它是一种跨平台，跨语言的服务。
我们可以这样理解它，比如说我们可以调用互联网上查询天气信息的web服务，把它嵌入到我们的B/S程序中，
当用户从我们的网点看到天气信息时，会认为我们为他提供很多的服务，但其实我们什么也没做，只是简单的调用了一下服务器上的一端代码而已。
Web service 可以将你的服务发布到互联网上让别人去调用，也可以调用别人发布的web service，和使用自己的代码一样。
它是采用XML传输格式化的数据，它的通信协议是SOAP(简单对象访问协议).







3.过滤器   filter的生命周期和servelt类似

对拦截的响应请求进行过滤，实质是对相应请求进行处理。



FilterConfig  主要是获取配置信息
http://blog.csdn.net/my98800/article/details/53940838





4.监听器
监听器实际上是一个类，这个类实现了特定的接口，然后将这个类在 web.xml 文件中进行描述，这样服务器在启动的时候就可以实例化这个类，启动监听器。当范围对象的状态发生变化的时候，服务器自动调用监听器对象中的方法。例如统计用户在线人数。





web监听器是Servlet规范中定义的一种特殊类，用于监听ServletContext，HttpSession,ServletRequest等域对象的创建、销毁、以及属性的变化等，可以在事件发生前、发生后进行一些处理。

 
监听器>过滤器>serlvet，在 web.xml 文件中配置的时候注意顺序









5.http状态码

200 -请求已成功，请求所希望的响应头或数据体将随此响应返回。


4xx（请求错误）这些状态码表示请求可能出错，妨碍了服务器的处理。

400 - 错误的请求。 服务器不理解请求的语法。
401 -（未授权）请求要求身份验证。对于登录后请求的网页，服务器可能返回此响应。
403 -对不起，您没有权限请求此连接！
404 - 未找到。 ·404.0 -（无） – 没有找到文件或目录
405 - 用来访问本页面的 HTTP 谓词不被允许（方法不被允许）如设置了get请求而却用了post


5xx（服务器错误） 这些状态码表示服务器在处理请求时发生内部错误。这些错误可能是服务器本身的错误，而不是请求出错。


500（服务器内部错误） 服务器遇到错误，无法完成请求。
6.http Content-Type 

Content-Type                            文件扩展名


application/octet-stream   ：          .*（ 二进制流，不知道下载文件类型）


text/html                  ：          .jsp，.html等



7.端口
https端口（443）
http:端口（80）









8.URI, URL, and URN

首先，URI，是uniform resource identifier，统一资源标识符，用来唯一的标识一个资源。而URL是uniform resource locator，统一资源定位器，
它是一种具体的URI，即URL可以用来标识一个资源，而且还指明了如何locate这个资源。而URN，uniform resource name，统一资源命名，是通过名字来标识资源，
比如mailto:java-net@java.sun.com。也就是说，URI是以一种抽象的，高层次概念定义统一资源标识，而URL和URN则是具体的资源标识的方式。URL和URN都是一种URI。




9.http method
GET
该GET方法请求指定资源的表示。请求使用GET只能检索数据。
HEAD
该HEAD方法要求与请求的响应相同GET，但没有响应体。
POST
该POST方法用于将实体提交给指定的资源，通常会导致服务器上的状态或副作用发生更改
PUT
该PUT方法使用请求有效载荷替换目标资源的所有当前表示。

DELETE
该DELETE方法删除指定的资源。
CONNECT
该CONNECT方法建立到由目标资源标识的服务器的隧道。

OPTIONS
该OPTIONS方法用于描述目标资源的通信选项。
TRACE
该TRACE方法沿着目标资源的路径执行消息环回测试。

PATCH
该PATCH方法用于对资源进行部分修改。








10.
接口地址由以下五部分组成：
Scheme	Address	Port	Context Path	Relative Path
取值说明：
Scheme（协议） = HTTP/HTTPS（默认HTTP）
Address  = IP地址/域名（可配置）
Port = 端口（可配置）
Context Path  = 上下文路径（可配置）
Relative Path = 服务相对路径



11.常见的参数
1.sessions：

一个sessionId可以唯一标识一个用户，这样就可以简单地进行用户识别









